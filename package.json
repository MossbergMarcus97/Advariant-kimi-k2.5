{
  "name": "advariant-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "worker"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w worker\"",
    "build": "npm run build -w frontend && npm run build -w worker",
    "deploy": "npm run deploy -w worker && npm run deploy -w frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
